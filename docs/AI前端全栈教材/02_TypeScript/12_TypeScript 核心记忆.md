# TypeScript 核心记忆

```text
类型声明
→ 类型推断
→ 类型收窄
→ 泛型
→ keyof
→ conditional type
→ infer
→ mapped type
→ utility types
→ discriminated union
→ runtime validation
```

这条线最终会直接连接 Vue、AI Tool Calling、Agent State 和后端 API 契约。
