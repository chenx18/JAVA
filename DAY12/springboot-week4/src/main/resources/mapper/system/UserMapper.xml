<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.week4.project.system.mapper.UserMapper">

  <resultMap id="UserResultMap" type="com.example.week4.project.system.domain.User">
    <id property="id" column="id" />
    <result property="userName" column="username" />
    <result property="nickName" column="nickname" />
    <result property="email" column="email" />
    <result property="status" column="status" />
  </resultMap>

  <select id="count" resultType="long">
    select count(*) from sys_use
    <where>
      <if test="user.userName != null and user.userName != ''">
        and username like concat('%', #{user.userName}, '%')
      </if>
       <if test="user.status != null">
        and status = #{user.status}
      </if>
    </where>
  </select>

  <select id="findAll" resultMap="UserResultMap">
    select id, username, nickname, email, status from sys_use 
  </select>

  <select id="findPage" resultMap="UserResultMap">
    select id, username, nickname, email, status
    from sys_use 
    <where>
      <if test="user.userName != null and user.userName != ''">
        name like concat('%',#{name},'%')
      </if>
      <if test="user.status != null">
        and status = #{user.status}
      </if>
    </where>
    order by id
    limit #{offset}, #{pageSize}
  </select>


  <select id="findById" parameterType="int" resultMap="UserResultMap">
    select id, username, nickname, email, status from sys_use where id = #{id}
  </select>


  <insert id="insert" parameterType="com.example.week4.project.system.domain.User" 
    useGeneratedKeys="true" keyProperty="id">
    insert into sys_use 
    ( username, nickname, email, status) 
    values 
    ( #{username}, #{nickname}, #{email}, #{status})
  </insert>

  <update id="updateById" parameterType="com.example.week4.project.system.domain.User">
    update sys_use 
    <set>
      <if test="username != null and username != ''">username = #{username},</if>
      <if test="nickname != null">nickname = #{nickname},</if>
      <if test="email != null">email = #{email},</if>
      <if test="status != null">status = #{status},</if>
    </set>
    where id = #{id}
  </update>

  <delete id="deleteById" parameterType="int">
    delete from sys_use where id = #{id}
  </delete>

  <delete id="deleteByIds">
    delete from sys_use
    where id in
    <foreach collection="ids" item="id" open="(" separator="," close=")">
      #{id}
    </foreach>
  </delete>

  <select id="existsById" resultType="boolean">
    select count(*) > 0 from sys_use where id = #{id}
  </select>

</mapper>

